# web-pic-browser
Local-only photo browser web app that runs on a localhost server. View images from selected folders with random display, favorites, tagging, and flexible sorting — fully offline and privacy-focused.
